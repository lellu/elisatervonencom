/*const createStore = () => {
  return new Vuex.Store({
    state: () => ({
      filter: 'all'
    }),
    mutations: {
      changeFilter (state, filter) {
        state.filter = filter;
      }
    }
  })
}

export default createStore*/
